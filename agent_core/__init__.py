"""agent_core — platform-agnostic domain layer for Argus.

Public API grows as phases are implemented. Upper layers (github_connect)
import from here; lower layers (llm_framework) are imported by agent_core.
"""
